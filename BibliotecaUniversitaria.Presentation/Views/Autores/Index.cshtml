@model IEnumerable<BibliotecaUniversitaria.Application.DTOs.AutorDTO>

@{
    ViewData["Title"] = "Autores";
}

<div class="container-fluid">
    <div class="row">
        <div class="col-12">
            <div class="card">
                <div class="card-header d-flex justify-content-between align-items-center">
                    <h3 class="card-title mb-0">
                        <i class="fas fa-user-edit me-2"></i>Gerenciar Autores
                    </h3>
                    <a asp-action="Create" class="btn btn-primary">
                        <i class="fas fa-plus me-1"></i>Novo Autor
                    </a>
                </div>
                <div class="card-body">
                    @if (TempData["SuccessMessage"] != null)
                    {
                        <div class="alert alert-success alert-dismissible fade show" role="alert">
                            <i class="fas fa-check-circle me-2"></i>@TempData["SuccessMessage"]
                            <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
                        </div>
                    }
                    @if (TempData["ErrorMessage"] != null)
                    {
                        <div class="alert alert-danger alert-dismissible fade show" role="alert">
                            <i class="fas fa-exclamation-circle me-2"></i>@TempData["ErrorMessage"]
                            <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
                        </div>
                    }

                    @if (Model.Any())
                    {
                        <div class="table-responsive">
                            <table class="table table-striped table-hover">
                                <thead class="table-dark">
                                    <tr>
                                        <th>Nome</th>
                                        <th>Nacionalidade</th>
                                        <th>Data de Nascimento</th>
                                        <th>Biografia</th>
                                        <th class="text-center">Ações</th>
                                    </tr>
                                </thead>
                                <tbody>
                                    @foreach (var autor in Model)
                                    {
                                        <tr>
                                            <td>
                                                <strong>@autor.Nome</strong>
                                            </td>
                                            <td>@(autor.Nacionalidade ?? "Não informado")</td>
                                            <td>@(autor.DataNascimento?.ToString("dd/MM/yyyy") ?? "Não informado")</td>
                                            <td>
                                                @if (!string.IsNullOrEmpty(autor.Biografia))
                                                {
                                                    <span class="text-truncate d-inline-block" style="max-width: 200px;" title="@autor.Biografia">
                                                        @autor.Biografia
                                                    </span>
                                                }
                                                else
                                                {
                                                    <span class="text-muted">Não informado</span>
                                                }
                                            </td>
                                            <td class="text-center">
                                                <div class="btn-group" role="group">
                                                    <a asp-action="Details" asp-route-id="@autor.Id" class="btn btn-sm btn-outline-info" title="Ver detalhes">
                                                        <i class="fas fa-eye"></i>
                                                    </a>
                                                    <a asp-action="Edit" asp-route-id="@autor.Id" class="btn btn-sm btn-outline-warning" title="Editar">
                                                        <i class="fas fa-edit"></i>
                                                    </a>
                                                    <a asp-action="Delete" asp-route-id="@autor.Id" class="btn btn-sm btn-outline-danger" title="Excluir">
                                                        <i class="fas fa-trash"></i>
                                                    </a>
                                                </div>
                                            </td>
                                        </tr>
                                    }
                                </tbody>
                            </table>
                        </div>
                    }
                    else
                    {
                        <div class="text-center py-5">
                            <i class="fas fa-user-edit fa-3x text-muted mb-3"></i>
                            <h5 class="text-muted">Nenhum autor cadastrado</h5>
                            <p class="text-muted">Comece adicionando um novo autor ao sistema.</p>
                            <a asp-action="Create" class="btn btn-primary">
                                <i class="fas fa-plus me-1"></i>Adicionar Primeiro Autor
                            </a>
                        </div>
                    }
                </div>
            </div>
        </div>
    </div>
</div>
